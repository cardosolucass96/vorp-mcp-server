# Kommo MCP Server - Configuração

# Porta do servidor (padrão: 3000)
PORT=3000

# Host para bind (padrão: 0.0.0.0 - aceita todas as conexões)
HOST=0.0.0.0

# Senha para autenticação Bearer (OBRIGATÓRIO - sem fallback)
# Formato do token: MCP_PASSWORD|subdomain|kommoToken
# IMPORTANTE: Use uma senha forte e única em produção!
# DEV: M0ra1s#3013
MCP_PASSWORD=SuaSenhaSegura123_AltereME
